<nav class="userBar navbar navbar-inverse">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <span class="glyphicon glyphicon-user"></span> {{ user.firstName }} {{ user.lastName }} ({{ user.email }}) <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('app_account_my_account') }}">Mon compte</a></li>

                        {% if user.isAdmin %}
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('admin') }}">Administration</a></li>
                        {% endif %}
                        {% if user.isSuperAdmin %}
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('app_switch_user_connect') }}">Changer d'utilisateur</a></li>
                        {% endif %}

                        {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('app_switch_user_logout') }}">Se déconnecter de ce compte</a></li>
                        {% endif %}

                        <li role="separator" class="divider"></li>

                        <li><a href="{{ path('app_disconnection') }}">Se déconnecter</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
